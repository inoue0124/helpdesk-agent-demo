# infrastructure — タスクリスト

## タスク

- [x] `docker-compose.yml`（ローカル開発用）
- [x] `docker-compose.prod.yml`（本番用）
- [ ] `.docker/Dockerfile`（Elasticsearch + kuromoji）
- [x] `Caddyfile`
- [x] `.env.example`
- [x] `backend/Dockerfile`
- [x] `frontend/Dockerfile`

## 依存関係

なし（他フィーチャーの前提となる基盤）
