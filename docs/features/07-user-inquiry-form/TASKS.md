# user-inquiry-form — タスクリスト

## タスク

- [ ] InquiryForm コンポーネント（フォーム入力 + バリデーション） — `frontend/app/components/InquiryForm.tsx`
- [ ] InquiryResult コンポーネント（受付完了 + ポーリング + 回答表示） — `frontend/app/components/InquiryResult.tsx`
- [ ] ユーザー画面（状態管理: form → waiting → answered） — `frontend/app/user/page.tsx`

## 依存関係

- 前提: api-endpoints（POST /api/inquiries, GET /api/inquiries/{id} が動作すること）
- 後続: なし
